---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description = "9th Grade Student Developer & Economics Enthusiast." } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#ffffff" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonicalURL} />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css" integrity="sha384-n8MVd4RsNIU0tAAbDJwPJzDEaqSD1odI+WdtXRGWt2kTvGFasHpSy3SV" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>{title}</title>
    <style is:global>
      /* Enhanced color variables - Modern book style */
      :root {
        --primary-light: #faf8f5;
        --primary-dark: #2c2926;
        --accent-light: #f5f2ed;
        --accent-dark: #3a3532;
        --text-light: #2c2926;
        --text-dark: #faf8f5;
        --text-muted-light: #5a544d;
        --text-muted-dark: #b8b0a6;
        --border-light: #e8e2d8;
        --border-dark: #3a3532;
        --shadow-sm: 0 1px 2px 0 rgba(44, 41, 38, 0.05);
        --shadow-md: 0 4px 6px -1px rgba(44, 41, 38, 0.1), 0 2px 4px -1px rgba(44, 41, 38, 0.06);
        --shadow-lg: 0 10px 15px -3px rgba(44, 41, 38, 0.1), 0 4px 6px -2px rgba(44, 41, 38, 0.05);
        --shadow-hover: 0 20px 25px -5px rgba(44, 41, 38, 0.1), 0 10px 10px -5px rgba(44, 41, 38, 0.04);
      }
      
      /* Global font family setting - Book typography */
      html, body, p, h1, h2, h3, h4, h5, h6, span, div, a, button, input, textarea, select, li, ul, ol, table, tr, td, th {
        font-family: "Georgia", "Times New Roman", Times, serif !important;
        font-weight: 400;
        letter-spacing: 0.01em;
      }
      
      /* Headings with classic book styling */
      h1, h2, h3, h4, h5, h6 {
        font-weight: 400;
        letter-spacing: 0.02em;
        margin-top: 2rem;
        margin-bottom: 1rem;
      }
      
      h1 { font-size: 2rem; }
      h2 { font-size: 1.5rem; }
      h3 { font-size: 1.25rem; }
      
      /* 确保图标字体不受影响 */
      .fa, .fas, .far, .fal, .fab, .fa-brands, .fa-solid, .fa-regular, .fa-light, i[class*="fa-"] {
        font-family: "Font Awesome 6 Free", "Font Awesome 6 Brands", sans-serif !important;
      }
      
      /* 确保表情符号正常显示 */
      .emoji, .emoji-container {
        font-family: "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji", sans-serif !important;
      }
      
      /* KaTeX color */
      .katex {
        font: inherit;
        color: inherit;
      }
      
      /* Hide scrollbar for Chrome, Safari and Opera */
      .scrollbar-hide::-webkit-scrollbar {
        display: none;
      }
      /* Hide scrollbar for IE, Edge and Firefox */
      .scrollbar-hide {
        -ms-overflow-style: none;  /* IE and Edge */
        scrollbar-width: none;  /* Firefox */
      }
      
      /* Responsive table styles */
      .table-container {
        width: 100%;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
      }
      
      /* Responsive images */
      img {
        max-width: 100%;
        height: auto;
        display: block;
      }
      
      /* Base font sizes and touch targets */
      html {
        font-size: 17px;
        -webkit-text-size-adjust: 100%;
        line-height: 1.7;
        scroll-behavior: smooth;
      }
      
      @media (max-width: 768px) {
        html {
          font-size: 16px;
        }
        
        h1 { font-size: 1.75rem; }
        h2 { font-size: 1.35rem; }
        h3 { font-size: 1.15rem; }
      }
      
      @media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }
        
        *, *::before, *::after {
          animation-duration: 0.01ms !important;
          animation-iteration-count: 1 !important;
          transition-duration: 0.01ms !important;
        }
      }
      
      /* Ensure minimum touch target sizes */
      a, button, input, select, textarea {
        min-height: 44px;
        min-width: 44px;
        padding: 0.5rem;
      }
      
      /* Fix horizontal scroll issues */
      body {
        overflow-x: hidden;
        width: 100%;
      }
      
      /* Improve text readability - Book style */
      p, li {
        line-height: 1.8;
        margin-bottom: 1.25rem;
        text-align: justify;
        hyphens: auto;
      }
      
      /* First letter styling for articles */
      article > p:first-of-type::first-letter {
        font-size: 3.5rem;
        float: left;
        line-height: 1;
        margin: 0.05em 0.1em 0 0;
        font-weight: 400;
      }
      
      /* Enhanced interactive elements - Modern book style */
      .interactive-element {
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        border-color: var(--border-light);
        position: relative;
      }
      
      .interactive-element:hover {
        background-color: var(--accent-light);
        box-shadow: var(--shadow-md);
        transform: translateY(-2px);
      }
      
      .interactive-element:active {
        transform: translateY(0);
        box-shadow: var(--shadow-sm);
      }
      
      
      /* Enhanced tag styles - Modern bookmark style */
      .unified-tag {
        display: inline-flex;
        align-items: center;
        padding: 0.25rem 0.75rem;
        border-radius: 4px;
        font-size: 0.8rem;
        line-height: 1.2;
        background-color: transparent;
        border: 1px solid var(--border-light);
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        font-style: italic;
      }
      
      .unified-tag:hover {
        background-color: var(--accent-light);
        border-color: var(--text-muted-light);
        box-shadow: var(--shadow-sm);
        transform: translateY(-1px);
      }
      
      
      /* Page styling */
      body {
        background-color: var(--primary-light);
        color: var(--text-light);
        position: relative;
      }
      
      /* Subtle background texture */
      body::before {
        content: '';
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: 
          repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(44, 41, 38, 0.01) 2px, rgba(44, 41, 38, 0.01) 4px);
        pointer-events: none;
        z-index: -1;
      }
      
      /* Links with underline */
      a {
        color: var(--text-light);
        text-decoration: underline;
        text-decoration-thickness: 1px;
        text-underline-offset: 0.2em;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }
      
      a:hover {
        text-decoration-thickness: 2px;
        text-decoration-color: var(--text-muted-light);
      }
      
      
      /* Remove underline from navigation */
      nav a, .unified-tag {
        text-decoration: none;
      }
      
      /* Enhanced focus states for accessibility */
      *:focus-visible {
        outline: 2px solid var(--text-light);
        outline-offset: 3px;
        border-radius: 2px;
      }
      
      button:focus-visible, a:focus-visible {
        outline: 2px solid var(--text-light);
        outline-offset: 3px;
      }
      
      /* Selection styling */
      ::selection {
        background-color: var(--text-light);
        color: var(--primary-light);
      }
      
      /* Smooth page transitions */
      @keyframes pageLoad {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      
      main {
        animation: pageLoad 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }
      
      /* Loading state */
      .loading {
        position: relative;
        pointer-events: none;
        opacity: 0.6;
      }
      
      .loading::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 20px;
        height: 20px;
        margin-top: -10px;
        margin-left: -10px;
        border: 2px solid var(--border-light);
        border-top-color: var(--text-light);
        border-radius: 50%;
        animation: spin 0.6s linear infinite;
      }
      
      @keyframes spin {
        to {
          transform: rotate(360deg);
        }
      }
    </style>
  </head>
  <body class="min-h-screen antialiased">
    <Header />
    <main class="max-w-4xl mx-auto px-4 py-8 md:py-12 min-h-[calc(100vh-64px-180px)]">
      <slot />
    </main>
    <Footer />
    
    <script>
      // Mobile menu toggle
      const menuToggle = document.getElementById('menu-toggle');
      const mobileMenu = document.getElementById('mobile-menu');
      const menuOpenIcon = document.querySelector('.menu-open');
      const menuCloseIcon = document.querySelector('.menu-close');
      
      if (menuToggle && mobileMenu && menuOpenIcon && menuCloseIcon) {
        menuToggle.addEventListener('click', () => {
          mobileMenu.classList.toggle('hidden');
          menuOpenIcon.classList.toggle('hidden');
          menuCloseIcon.classList.toggle('hidden');
        });
      }
      
      // Close mobile menu when clicking outside
      document.addEventListener('click', (e) => {
        if (mobileMenu && !mobileMenu.contains(e.target as Node) && !menuToggle?.contains(e.target as Node)) {
          mobileMenu.classList.add('hidden');
          menuOpenIcon?.classList.remove('hidden');
          menuCloseIcon?.classList.add('hidden');
        }
      });
      
      // Handle table responsiveness
      document.querySelectorAll('table').forEach(table => {
        const wrapper = document.createElement('div');
        wrapper.className = 'table-container scrollbar-hide';
        table.parentNode.insertBefore(wrapper, table);
        wrapper.appendChild(table);
      });
    </script>
  </body>
</html>